import React from "react";
import styles from "./authentication.module.css";
const Authentication = () => {
  return <h1 className={styles.title}>this is authentication</h1>;
};
export default Authentication;
